--- a/example/convex/convex.config.ts
+++ b/example/convex/convex.config.ts
@@ -2,6 +2,12 @@
 import workpool from "@convex-dev/workpool/convex.config";
 import workpoolOld from "@convex-dev/workpool-old/convex.config";
 import staticHosting from "@convex-dev/static-hosting/convex.config";
+
+import baseline from "../../benchmark-components/baseline/src/component/convex.config";
+
+import ordering from "../../benchmark-components/ordering/src/component/convex.config";
+
+import packed from "../../benchmark-components/packed/src/component/convex.config";
 
 const app = defineApp({ httpPrefix: "/api" });
 app.use(workpool, { name: "smallPool" });
@@ -11,4 +17,10 @@
 app.use(workpoolOld, { name: "oldWorkpool" });
 app.use(staticHosting, { httpPrefix: "/" });
 
+app.use(baseline, { name: "benchBaseline0909" });
+
+app.use(ordering, { name: "benchOrdering0909" });
+
+app.use(packed, { name: "benchPacked0909" });
+
 export default app;
--- a/example/convex/test/pool.ts
+++ b/example/convex/test/pool.ts
@@ -9,9 +9,9 @@
 } from "@convex-dev/workpool-old";
 import { ActionCtx, MutationCtx } from "../_generated/server";
 
-export const POOL_KINDS = ["new", "old"] as const;
+export const POOL_KINDS = ["new", "old", "baseline", "ordering", "packed"] as const;
 export type PoolKind = (typeof POOL_KINDS)[number];
-export const vPoolKind = v.union(v.literal("new"), v.literal("old"));
+export const vPoolKind = v.union(...POOL_KINDS.map((kind) => v.literal(kind)));
 
 type Ctx = ActionCtx | MutationCtx;
 
@@ -20,8 +20,7 @@
   kind: PoolKind,
   maxParallelism: number,
 ): Promise<void> {
-  const component =
-    kind === "new" ? components.testWorkpool : components.oldWorkpool;
+  const component = getComponent(kind);
   await ctx.runMutation(component.config.update, { maxParallelism });
 }
 
@@ -34,12 +33,12 @@
   kind: PoolKind,
   opts: { maxParallelism: number },
 ): Workpool {
-  if (kind === "new") return new Workpool(components.testWorkpool, opts);
+  if (kind !== "old") return new Workpool(getComponent(kind), opts);
   return new OldWorkpool(components.oldWorkpool, opts) as Workpool;
 }
 
 export function getComponent(kind: PoolKind) {
-  return kind === "new" ? components.testWorkpool : components.oldWorkpool;
+  return { new: components.testWorkpool, old: components.oldWorkpool, baseline: components.benchBaseline0909, ordering: components.benchOrdering0909, packed: components.benchPacked0909 }[kind];
 }
 
 export function enqueueFor(kind: PoolKind): {
@@ -47,9 +46,9 @@
   enqueueOne: typeof enqueue;
   enqueueMany: typeof enqueueBatch;
 } {
-  if (kind === "new") {
+  if (kind !== "old") {
     return {
-      component: components.testWorkpool,
+      component: getComponent(kind),
       enqueueOne: enqueue,
       enqueueMany: enqueueBatch,
     };
--- a/example/convex/schema.ts
+++ b/example/convex/schema.ts
@@ -51,7 +51,7 @@
   // rather than inferred from completion times.
   latencyTasks: defineTable({
     cell: v.string(), // which experiment cell enqueued it
-    pool: v.union(v.literal("new"), v.literal("old")),
+    pool: v.union(v.literal("new"), v.literal("old"), v.literal("baseline"), v.literal("ordering"), v.literal("packed")),
     delayMs: v.number(), // 0 for immediate
     seq: v.number(), // enqueue order within the cell
     enqueuedAt: v.number(), // clock at the enqueuing mutation
--- a/example/convex/test/run.ts
+++ b/example/convex/test/run.ts
@@ -33,7 +33,7 @@
   args: {
     scenario: v.string(),
     parameters: v.any(),
-    pool: v.optional(v.union(v.literal("new"), v.literal("old"))),
+    pool: v.optional(v.union(v.literal("new"), v.literal("old"), v.literal("baseline"), v.literal("ordering"), v.literal("packed"))),
   },
   handler: async (ctx, args) => {
     // Check for in-flight tasks from the latest run
@@ -127,7 +127,7 @@
 
 export const latestRunForPoolSince = internalQuery({
   args: {
-    pool: v.union(v.literal("new"), v.literal("old")),
+    pool: v.union(v.literal("new"), v.literal("old"), v.literal("baseline"), v.literal("ordering"), v.literal("packed")),
     since: v.number(),
   },
   returns: v.union(v.id("runs"), v.null()),
